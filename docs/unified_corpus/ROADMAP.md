# Roadmap

- **Web ingestion pipeline**: discover→fetch→distill→digest→validate→SNAPDNA
- **Persona learning loop**: EMA + bandit exploration, stance filters
- **SNAPBIT stitcher v1**: AST-level stitching, import resolver, style/lint, test runner
- **Playbooks v1**: action execution (not just annotations), sandboxed verify hooks
- **Provenance**: link artifacts under a `FunctionLifecycle` doc; render graphs
- **Vec store integration**: embeddings for retrieval/semantic routing
- **MoE agents**: teacher/worker loop feeding into AGRM/Hash (separate package)
